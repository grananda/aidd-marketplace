.DS_Store
node_modules/

# Python bytecode (compilado, artefacto derivado; se regenera solo)
__pycache__/
*.py[cod]

# Paquete de referencia de upstream, descomprimido para comparar versiones.
# Material de otro proyecto: no forma parte del marketplace.
native-ai-specs-v*/
native-ai-specs-v*.zip
DF-Samples/
Pruebas-Samples/
# Informe de trabajo de la auditoria de skills. Se regenera; no es del marketplace.
Auditoria/
figma/
# Documentos de cliente para depurar el DF. Nunca al repo.
DF/
# Material de origen de Native AI y los informes derivados (HTML, PDF, PPTX).
# Documentacion de otro proyecto y entregables que se regeneran: no son del marketplace.
native-ia-source/
